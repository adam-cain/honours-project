export default function NotFound(text: React.ReactNode) {
    return (
        <div className="flex justify-center size-full rounded align-middle">
            <p className="text-gray-400 m-auto">{text}</p>
        </div>
    )
}